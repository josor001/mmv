package de.fhdo.hmmm.utility.visualizer

/**
 * Enum which holds all valid output formats for the visualization.
 * Currently, only the SVG format is supported.
 *
 * @author Jonas Sorgalla
 */
enum class EOutputFormat {
    SVG
}